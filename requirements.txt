# Pinned-ish runtime deps (also declared in pyproject.toml).
# Use `pip install -e .[dev]` for the full dev environment instead of this file.
fastapi>=0.110
uvicorn[standard]>=0.29
sqlalchemy>=2.0
geoalchemy2>=0.14
alembic>=1.13
psycopg[binary]>=3.1
pydantic>=2.6
pydantic-settings>=2.2
websockets>=12.0
shapely>=2.0
httpx>=0.27

# dev
pytest>=8.0
pytest-asyncio>=0.23
